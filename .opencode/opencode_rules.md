1. Read full files — when Codex mentions any file, read the full file plus all related/relevant files, not just sections
2. Read action_log2.md and task.md before starting — for planner context and decisions
3. No half results — trace every claim to source, don't skim
4. Trace every layer — when a parameter is flagged, follow it through all hops (schema → handler → doc_store method → output), not just the first one or two
5. Ponytail active — ladder first, shortest diff, no speculative code
6. Only edit files task.md says are allowed — and only the specific sections mentioned
7. Plan with file map — in every task plan, list each file to change with its path, the exact code block to modify, and line numbers. This gives Codex a visual diff preview for review before execution.
